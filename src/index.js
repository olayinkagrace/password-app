import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import RandomPassword from './RandomPassword';


const root = ReactDOM.createRoot(document.getElementById("root"))

const element = <RandomPassword />
root.render(element)

